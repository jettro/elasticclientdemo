{
    "query":{
{% if (length(name)==0) %}
        "match_all": {}
{% else %}
        "match":{
            "name.ngram": {
                "query": "{{ name }}",
                "operator": "{{ default(operator, 'or') }}"
            }
        }
{% endif %}
    }
}
